{
  "name": "savecontext",
  "version": "0.1.16",
  "description": "SaveContext - Never lose context in your AI coding sessions",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "serve": "npm run serve -w server",
    "start": "npm run start -w server",
    "version:patch": "npm version patch --no-git-tag-version && npm version patch --no-git-tag-version -w server",
    "version:minor": "npm version minor --no-git-tag-version && npm version minor --no-git-tag-version -w server",
    "version:major": "npm version major --no-git-tag-version && npm version major --no-git-tag-version -w server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/greenfieldlabs-inc/savecontext.git"
  },
  "author": "Shane Monastero",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
